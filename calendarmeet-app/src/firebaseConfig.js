// FIREBASE DISABLED: app uses AWS Cognito for auth and Postgres for data.
// These stubs keep Firebase imports from breaking while the calendar migrates.
export const db = null;
export const auth = null;
export const googleProvider = null;
export const hasAllConfig = false;
