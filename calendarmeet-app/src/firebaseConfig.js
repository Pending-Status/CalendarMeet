// FIREBASE TEMPORARILY DISABLED - Using AWS Cognito for auth
// Firebase API keys are commented out in .env, causing initialization errors
// Calendar functionality will be migrated to use PostgreSQL backend instead

// Temporary stubs to prevent import errors in CalendarPage
export const db = null;
export const auth = null;
export const googleProvider = null;
